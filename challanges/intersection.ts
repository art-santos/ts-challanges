export default function intersection<T>(...arrays: Array<Array<T>>): Array<T> {
    throw 'Not implemented!';
}

const intersection_number = intersection([1, 2, 3], [2, 3, 4], [3, 4, 5]);
console.log(intersection_number);

